{
  "name": "business-task-manager",
  "version": "1.0.0",
  "description": "Business task sharing application with Google OAuth and PostgreSQL",
  "main": "index.html",
  "scripts": {
    "dev": "netlify dev",
    "dev-simple": "npx http-server . -p 3000",
    "build": "echo 'Static build - no build process required'"
  },
  "dependencies": {
    "@netlify/functions": "^2.0.0",
    "cors": "^2.8.5",
    "dotenv": "^17.2.2",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "http-server": "^14.1.1",
    "netlify-cli": "^23.5.0"
  }
}
